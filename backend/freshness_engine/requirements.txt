# Freshness Score Engine — isolated microservice dependencies
# Install with: pip install -r requirements.txt
fastapi==0.115.14
uvicorn[standard]==0.35.0
pydantic>=2.0.0            # v2 — field_validator syntax used in api.py
pytest==8.4.1
httpx==0.28.1              # needed for FastAPI TestClient in integration tests
starlette==0.46.2
